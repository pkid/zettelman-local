# Evaluation

`sample_expectations.json` stores the expected extraction output for the local reference zettels.

Local sample images are intentionally not committed because they contain personal data.
Use `Scripts/prepare_local_samples.sh` to copy the local-only HEIC files into `Evaluation/.local-samples/`.
